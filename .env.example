# Variables de entorno para desarrollo local
# Copia este archivo a .env.local para usar localmente

# Base de Datos
DB_HOST=localhost
DB_PORT=3306
DB_NAME=productos_db
DB_USER=root
DB_PASSWORD=root

# Servidor
SERVER_PORT=8080

# Perfil de Spring
SPRING_PROFILES_ACTIVE=dev
